const enabled = true;

if (enabled) {
    VANTA.RINGS({
        el: "#app",
        mouseControls: true,
        touchControls: true,
        gyroControls: true,
        minHeight: 200.00,
        minWidth: 200.00,
        scale: 0.50,
        scaleMobile: 1.00,
        backgroundColor: 0xf0f0f0,
        color: 0xf0f0f0
    });
}